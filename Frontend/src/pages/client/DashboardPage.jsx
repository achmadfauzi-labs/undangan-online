export function ClientDashboardPage() {
  return (
    <div>
      <div className="page-header">
        <h2 className="page-title">Dashboard</h2>
        <p className="page-subtitle">Ringkasan undangan dan tamu</p>
      </div>
      <div className="card">
        <p style={{ color: 'var(--color-text-muted)' }}>Dashboard client akan ditampilkan di sini.</p>
      </div>
    </div>
  );
}
