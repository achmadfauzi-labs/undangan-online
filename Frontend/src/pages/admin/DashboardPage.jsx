export function AdminDashboardPage() {
  return (
    <div>
      <div className="page-header">
        <h2 className="page-title">Dashboard</h2>
        <p className="page-subtitle">Ringkasan aktivitas sistem</p>
      </div>
      <div className="card">
        <p style={{ color: 'var(--color-text-muted)' }}>Dashboard admin akan ditampilkan di sini.</p>
      </div>
    </div>
  );
}
